(function() {
  'use strict';

  angular
    .module('app')
    .service('theme003Service', theme003Service);

  function theme003Service() {
    var self = this;

    // self.supports(needs) {
    //   return (needs.framework == 'unity' && needs.colorScheme == 'red');
    // }

  }

})();
